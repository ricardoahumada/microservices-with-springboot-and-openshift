<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- TODO Ejercicio 1.2: Configurar logs estructurados JSON -->
    
    <!-- 
    PASO 1: Definir propiedades de Spring
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="afiliado-service"/>
    <springProperty scope="context" name="appVersion" source="app.version" defaultValue="1.0.0"/>
    -->

    <!-- Configuracion basica de consola (sin JSON) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 
    TODO Ejercicio 1.2: Reemplazar el appender CONSOLE basico por uno con LogstashEncoder
    
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <includeMdcKeyName>requestId</includeMdcKeyName>
            <includeMdcKeyName>clientIp</includeMdcKeyName>
            <customFields>{"service":"${appName}","version":"${appVersion}","environment":"${ENVIRONMENT:-dev}"}</customFields>
        </encoder>
    </appender>
    -->

    <!-- 
    TODO Ejercicio 1.2: Agregar File Appender para logs JSON persistentes
    
    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./logs/${appName}.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>./logs/${appName}-%d{yyyy-MM-dd}.json</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <includeMdcKeyName>requestId</includeMdcKeyName>
            <includeMdcKeyName>clientIp</includeMdcKeyName>
            <customFields>{"service":"${appName}","version":"${appVersion}","environment":"${ENVIRONMENT:-dev}"}</customFields>
        </encoder>
    </appender>
    -->

    <!-- 
    TODO Ejercicio 3.3: Agregar Audit Logger separado para dashboard en Kibana
    
    <appender name="AUDIT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./logs/audit.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>./logs/audit-%d{yyyy-MM-dd}.json</fileNamePattern>
            <maxHistory>90</maxHistory>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"${appName}","logType":"AUDIT"}</customFields>
        </encoder>
    </appender>

    <logger name="AUDIT" level="INFO" additivity="false">
        <appender-ref ref="AUDIT_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>
    -->

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <!-- TODO Ejercicio 1.2: Agregar JSON_FILE cuando este configurado -->
        <!-- <appender-ref ref="JSON_FILE"/> -->
    </root>
</configuration>
